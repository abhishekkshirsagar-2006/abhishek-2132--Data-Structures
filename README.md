# library-record-system
A Python program to analyze library borrowing records using lists and for loops. It calculates the average, highest, lowest, zero borrow count, and mode of books borrowed by library members.
